# Script for computing bivariate statistics between two ordinal variables
# (with weights)

#A <- Ordinal
#B <- Ordinal
#WM <- Weights

A = read("in/educ.ordinal.data", rows=474, cols=1, format="text");
B = read("in/jobcat.ordinal.data", rows=474, cols=1, format="text");
WM = read("in/jobcat.ordinal.data", rows=$$rows$$, cols=1, format="text");

sp = spearman(A,B,WM);

print("Spearman correlation = ", sp);

