V = read("gnmf/in/v", rows=10, cols=8, format="text");
W = read("gnmf/in/w", rows=10, cols=5, format="text");
H = read("gnmf/in/h", rows=5, cols=8, format="text");
max_iteration = 1;
i = 0;

Eps = 10^-8;

while(i < max_iteration) {
	H = H * ((t(W) %*% V) / (((t(W) %*% W) %*% H)+Eps)) ;
	W = W * ((V %*% t(H)) / ((W %*% (H %*% t(H)))+Eps));
	i = i + 1;
}

write(W, "gnmf/out/w", format="text");
write(H, "gnmf/out/h", format="text");
